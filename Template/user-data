#cloud-config
ssh_pwauth: True
{% if sshKeyArray is defined %}
ssh_authorized_keys:
{% for sshKey in sshKeyArray %}
  - {{ sshKey }}
{% endfor %}
{% endif %}
users:
  - default
  {% for user in userArray %}
  - name: {{ user.name + index}}
    gecos: {{ user.name.upper() }}
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    ssh_pwauth: True
  {% endfor %}
chpasswd:
  list: |
     ubuntu:ubuntu
  expire: False
final_message: "Credenciales:\nuser:ubuntu\npassword:ubuntu\n"
